{
  "name": "analytics-hub-video-stats",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy",
    "dev": "node build.js && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
